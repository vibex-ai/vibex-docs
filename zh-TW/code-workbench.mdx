---
title: "程式碼工作台"
description: "瀏覽、搜尋、編輯與預覽工作區檔案。"
---

程式碼工作台把檔案樹、搜尋、編輯器、Git 差異與內容預覽放在同一個視窗。所有檔案操作以目前工作區為邊界。

## 檔案樹與搜尋

1. 開啟側欄的檔案樹並展開工作區目錄。
2. 用檔名搜尋定位路徑。
3. 用內容搜尋查找文字；結果會顯示路徑與匹配位置。
4. 點擊檔案，在預覽分頁開啟。多個分頁可並列或分欄顯示。

大型檔案、無法解碼的內容和工作區外路徑可能被檔案服務拒絕。

## 編輯與儲存

- 在編輯器直接修改文字。
- 使用 **Save** 儲存檔案；快捷鍵為 `cmd-s`（Windows/Linux 通常為 `ctrl-s`）。
- 新建、重新命名或刪除檔案前確認目標路徑。
- 若檔案已被外部程序修改，先重新載入並比較差異。

<Warning>
  刪除檔案會改變工作區內容。刪除前查看 Git 狀態，必要時先建立提交或備份。
</Warning>

## 內容預覽

| 類型 | 能做什麼 |
| --- | --- |
| Markdown | 在編輯與渲染視圖切換，查看程式碼、表格、公式與圖表。 |
| 圖片 | 查看圖片並進入圖片編輯器，支援復原與重做。 |
| PDF | 使用原生 PDF 預覽逐頁查看。 |
| Office 文件 | 預覽受支援的 Office 內容。 |
| 其他文字 | 以編輯器開啟並保留編碼與換行。 |

<Frame caption="截圖佔位：檔案樹、編輯器、Markdown 預覽與多分頁配置">
  <img src="/images/screenshot-placeholder.svg" alt="程式碼工作台截圖佔位" />
</Frame>

## 圖片編輯與分欄

圖片編輯器提供 macOS 的 `cmd-z`/`cmd-shift-z` 和 Windows/Linux 的 `ctrl-z`/`ctrl-shift-z` 復原/重做。儲存前確認輸出路徑與格式。拖動分頁或使用分欄入口，可以同時查看程式碼、差異、終端機與文件。

## 常用快捷鍵

| 操作 | macOS | Windows/Linux |
| --- | --- | --- |
| 儲存檔案 | `cmd-s` | `ctrl-s` |
| 開啟預覽 | `cmd-shift-p` | `ctrl-shift-p` |
| 側欄 | `cmd-b` | `ctrl-b` |
| 前進/後退 | `alt-left` / `alt-right` | `alt-left` / `alt-right` |
